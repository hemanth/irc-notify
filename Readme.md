__To get up and running :__

* Indeed you must have node.

* `npm install irc`

* Check out the above code and edit `config.js` as per need.

* Finally start the bot `node notify.js` and the pm notifications ;) 

